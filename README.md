# Notas

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es el numero del cual se va a crear la tabla[number] [required]
  -l, --listar   Imprime la tabla creada en consola   [boolean] [default: false]
  -h, --hasta    Hasta donde quieres realizar la multiplicacion
                                                          [number] [default: 10]
```